[project]
name = "okf-els-api"
version = "0.2.0"
description = "Metadata-only OKF bundle for the Explore Local Statistics internal API documentation"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py311"
